<h1 style="text-align: center;">Get in touch</h1>
<form class="contact-form" action="https://formspree.io/f/mzbklljo" method="POST">
<div class="Name">
    <input type="text" id="Name" name="Name" class="nameinput" placeholder="Name">
</div>
<div class="Email">
    <input type="email" id="Email" name="Email" class="emailinput" placeholder="Email">
</div>
<div class="Message">
    <textarea id="Message" name="Message" placeholder="Message"></textarea>
</div>
<div>
    <span>
    <input type="submit" value="Submit" class="btn">
    </span>
</div>
</form>

<style>
	:root {
		--color-bg-textarea: rgba(0, 0, 0, .6);
	}

	.contact-form {
		margin-top: 2rem;
		display: grid;
		grid-template-columns: minmax(300px, 600px);
		justify-content: center;
		padding-left: 1rem;
		padding-right: 1rem;
	}

	textarea {
		background: var(--color-bg-textarea);
		width: 100%;
		height: 100%;
		border: none;
		border-radius: 10px;
		background-clip: padding-box;
		display: block;
		font-size: 18px;
		color: #fff;
		padding-left: 1rem;
		padding-right: 20px;
		padding-top: 12px;
		margin-bottom: 20px;
		overflow: hidden;
	}

	#Message {
		height: 10rem;
	}

	select {
		line-height: 1.5;
		font-size: 1.4em;
		border: none;
		border-radius: 10px;
		color: #fff;
		display: block;
		background: transparent;
		background-color: var(--color-bg-textarea);
		margin-bottom: 20px;
		display: block;
		font-size: 18px;
		appearance: none;
	}

	.nameinput,
	.emailinput {
		width: 100%;
		height: 48px;
		border: none;
		-moz-border-radius: 10px;
		-webkit-border-radius: 10px;
		border-radius: 10px;
		-moz-background-clip: padding;
		-webkit-background-clip: padding-box;
		background-clip: padding-box;
		display: block;
		font-size: 18px;
		color: #fff;
		padding-left: 20px;
		padding-right: 20px;
		margin-bottom: 20px;
	}

	input[type=submit] {
		cursor: pointer;
	}

	input {
		&.nameinput {
			background: var(--color-bg-textarea);
			padding-left: 1rem;
		}
		&.emailinput {
			background: var(--color-bg-textarea);
			padding-left: 1rem;
		}
		&.message {
			background: var(--color-bg-textarea);
			padding-left: 1rem;
		}
	}

	select.indent {
		padding-left: 45px;
		cursor: pointer;
	}

	input:focus,
	textarea:focus {
		background-color: rgba(0, 0, 0, 0.2);
		box-shadow: 0 0 5px 1px rgba(255, 255, 255, .5);
		overflow: hidden;
	}

	.btn {
		margin-top: 1rem;
		padding: 1rem 6rem 1rem;
		text-align: center;
		font-size: 24px;
		color: transparent;
		text-shadow: 0px 0px 1px var(--color-primary);
		border: 4px var(--color-primary) solid;
		background: rgba(0, 0, 0, 0.4); 
		cursor: pointer;

		&:hover,
		&:active {
			background: var(--color-primary);
			outline: none;
			box-shadow: 0 0 var(--color-primary);
			top: 2px;
			color: white;
		}
	}

	input {
		-webkit-appearance: none;
		-moz-appearance: none;
		appearance: none;
		opacity: 1;
	}
</style>