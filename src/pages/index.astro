<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Conveyor Belt Brand Scroll</title>
<style>
  body {
    font-family: sans-serif;
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 50vh; /* For demonstration purposes */
    background-color: #f0f0f0; /* For demonstration purposes */
    margin: 0;
  }

  .conveyor-belt-wrapper {
    max-width: 800px; /* Adjust to your desired max width */
    margin: 20px auto;
  }

  .conveyor-belt-wrapper h2 {
    text-align: center;
    margin-bottom: 20px;
  }

  .conveyor-belt {
    width: 100%;
    overflow: hidden;
    position: relative;
    border: 1px solid #ddd; /* Optional: for visualization */
    background-color: #fff; /* Optional: background for the belt */
    padding: 10px 0; /* Optional: padding inside the belt */
  }

  .conveyor-track {
    display: flex;
    width: fit-content; /* Allows the track to be as wide as its content */
    animation: scroll 20s linear infinite; /* Adjust duration for speed */
  }

  .brand-item {
    flex-shrink: 0; /* Prevent items from shrinking */
    padding: 0 25px; /* Adjust spacing between items */
    display: flex;
    align-items: center;
    justify-content: center;
    box-sizing: border-box;
  }

  .brand-item img {
    max-width: 120px; /* Adjust max brand logo width */
    max-height: 60px; /* Adjust max brand logo height */
    display: block;
    object-fit: contain; /* Ensures the whole logo is visible */
    opacity: 0.8; /* Optional: visual style for logos */
    transition: opacity 0.3s ease;
  }

  .brand-item img:hover {
    opacity: 1; /* Optional: highlight logo on hover */
  }

  @keyframes scroll {
    0% {
      transform: translateX(0);
    }
    100% {
      /* Move the track by the width of the first set of items.
         If you have N original items, and you duplicate them once,
         the track moves by N * (width_of_one_item_including_padding).
         translateX(-50%) works because the track becomes 200% of the
         width of the original set of items after duplication. */
      transform: translateX(-50%);
    }
  }

  /* Optional: Pause animation on hover */
  .conveyor-belt:hover .conveyor-track {
    animation-play-state: paused;
  }
</style>
</head>
<body>

<div class="conveyor-belt-wrapper">
  <h2>Our Brands</h2>
  <div class="conveyor-belt">
    <div class="conveyor-track">
      <div class="brand-item"><img src="https://via.placeholder.com/120x60/007bff/ffffff?text=Brand+1" alt="Brand 1"></div>
      <div class="brand-item"><img src="https://via.placeholder.com/120x60/28a745/ffffff?text=Brand+2" alt="Brand 2"></div>
      <div class="brand-item"><img src="https://via.placeholder.com/120x60/ffc107/000000?text=Brand+3" alt="Brand 3"></div>
      <div class="brand-item"><img src="https://via.placeholder.com/120x60/dc3545/ffffff?text=Brand+4" alt="Brand 4"></div>
      <div class="brand-item"><img src="https://via.placeholder.com/120x60/17a2b8/ffffff?text=Brand+5" alt="Brand 5"></div>

      <div class="brand-item"><img src="https://via.placeholder.com/120x60/007bff/ffffff?text=Brand+1" alt="Brand 1"></div>
      <div class="brand-item"><img src="https://via.placeholder.com/120x60/28a745/ffffff?text=Brand+2" alt="Brand 2"></div>
      <div class="brand-item"><img src="https://via.placeholder.com/120x60/ffc107/000000?text=Brand+3" alt="Brand 3"></div>
      <div class="brand-item"><img src="https://via.placeholder.com/120x60/dc3545/ffffff?text=Brand+4" alt="Brand 4"></div>
      <div class="brand-item"><img src="https://via.placeholder.com/120x60/17a2b8/ffffff?text=Brand+5" alt="Brand 5"></div>
    </div>
  </div>
</div>

</body>
</html>
